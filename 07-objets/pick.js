// creates an object composed of the picked object properties. 

function pick (obj,tab) {
    //creer un objet vide et y mettre les propriétés demandées
    const newObj = {}
    for (let i=0;i<tab)
}


const pick = {
    source: ,
    property :
},

console.log(pick({a:1,v:"2",c:3}, ["a","c"])) // doit afficher {a: 1, c: 3}